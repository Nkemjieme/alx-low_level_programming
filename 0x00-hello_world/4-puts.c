#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char joel[52] = "\"Programming is like building a multilingual puzzle";

	puts(joel);
	return (0);
}
